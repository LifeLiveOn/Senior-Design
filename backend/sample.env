# === OAuth ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# === Django Core ===
SECRET_KEY=
DEBUG=1

# === Hosts & Domains (CRITICAL) ===
ALLOWED_HOSTS=
SITE_URL=

# === CSRF (MANDATORY for Django 4+ HTTPS) ===
CSRF_TRUSTED_ORIGINS=https:app.com,http://localhost:8000

# === CORS ===
CORS_ALLOWED_ORIGINS=https:app.com,http://localhost:8000

# === Cookies / Security ===
SECURE_SSL_REDIRECT=0
SESSION_COOKIE_SECURE=1
CSRF_COOKIE_SECURE=1
SESSION_COOKIE_SAMESITE=Lax
CSRF_COOKIE_SAMESITE=Lax
COOKIE_DOMAIN=app.com

# === Database ===
DB_ENGINE=django.db.backends.postgresql
DB_HOST=
DB_NAME=
DB_USER=
DB_PASS=
DB_PORT=
DB_SSLMODE=require

# === Storage ===
BUCKET_NAME=roofimage
